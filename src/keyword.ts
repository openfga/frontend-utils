export enum Keyword {
  TYPE = "type",
  RELATIONS = "relations",
  SELF = "self",
  DEFINE = "define",
  AS = "as",
  OR = "or",
  AND = "and",
  FROM = "from",
  BUT_NOT = "but not",
  COMMENT = "comment",
  MODEL = "model",
  SCHEMA = "schema",
  TYPE_RESTRICTIONS = "type-restrictions",
}

export enum ReservedKeywords {
  THIS = "this",
}
