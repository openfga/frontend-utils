export enum Keyword {
  TYPE = "type",
  RELATIONS = "relations",
  SELF = "self",
  DEFINE = "define",
  AS = "as",
  OR = "or",
  AND = "and",
  FROM = "from",
  BUT_NOT = "but not",
  MODEL = "model",
  SCHEMA = "schema",
}

export enum ReservedKeywords {
  THIS = "this",
}
