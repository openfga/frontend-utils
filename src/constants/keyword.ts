export enum Keyword {
  TYPE = "type",
  RELATIONS = "relations",
  SELF = "self",
  DEFINE = "define",
  AS = "as",
  OR = "or",
  AND = "and",
  FROM = "from",
  WITH = "with",
  BUT_NOT = "but not",
  MODEL = "model",
  SCHEMA = "schema",
  CONDITION = "condition",
}

export enum ReservedKeywords {
  THIS = "this",
}
